import { ScenarioMutations } from './mutations';
import { ScenarioDefaultState, IScenarioState } from './state';

describe('ScenarioMutations', () => {
  let testState: IScenarioState;

  beforeEach(() => {
    testState = ScenarioDefaultState();
  });

  test('Please write the tests for the mutations here', () => {
    // here is an example: https://github.com/vuesion/vuesion/blob/master/src/app/counter/mutations.spec.ts
    expect(true).toBeFalsy();
  });
});
