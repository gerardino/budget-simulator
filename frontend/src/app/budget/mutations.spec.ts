import { BudgetMutations } from './mutations';
import { BudgetDefaultState, IBudgetState } from './state';

describe('BudgetMutations', () => {
  let testState: IBudgetState;

  beforeEach(() => {
    testState = BudgetDefaultState();
  });

  test('Please write the tests for the mutations here', () => {
    // here is an example: https://github.com/vuesion/vuesion/blob/master/src/app/counter/mutations.spec.ts
    expect(true).toBeFalsy();
  });
});
